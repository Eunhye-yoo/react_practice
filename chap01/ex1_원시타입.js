//변수 - 원시(기초)타입

// 1. Number Type
let num1 = 20;
let num2 = 3.14;
let num3 = -3;

let inf = Infinity; //양의 무한대
let minf = -Infinity; //음의 무한대

let nan = NaN; //(Not a Number)지만 숫자타입, 수치 연산 실패시 반환되는 값

// 2. String = "", ''
let name = "홍은서";

//  backtick : `` 변수값을 동적으로 넣을 수 있는 따옴표
// 템플릿 리터럴 문법
console.log(`내 이름은 ${name}입니다.`);

// 3. Null Type : (값 존재X), 개발자가 직접 값을 대입
let empty = mull;

// 4. Undefined Type : 변수를 초기화하지않고 선언만 할 경우 자동 대입
let none;
